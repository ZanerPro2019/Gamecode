for bx in range(0, 1120, 70):
        boxes.add(Box(bx, 100))
    for bx in range(1050, 1120, 70):
        boxes.add(Box(bx, 170))
    for bx in range(1050, 1120, 70):
        boxes.add(Box(bx, 240))
    for bx in range(1050, 1120, 70):
        boxes.add(Box(bx, 310))
    for bx in range(910, 1120, 70):
        boxes.add(Box(bx, 380))
    for bx in range(0, 700, 70):
        boxes.add(Box(bx, 450))
    for bx in range(0, 280, 70):
        boxes.add(Box(bx, 590))
    for bx in range(770, 1120, 70):
        boxes.add(Box(bx, 660))
    for bx in range(0, 630, 70):
        boxes.add(Box(bx, 790))
    #лава
    for bx in range(0, 420, 70):
        boxes.add(Box(bx, 930))
    for bx in range(420, 1120, 70):
        boxes.add(Box(bx, 930))
